query Video($id: String!) {
  Video_getById(id: $id) {
    id
    user_id
    user_name
    title
    description
    created_at
    published_at
    url
    thumbnail_url
    view_count
    duration
  }
}